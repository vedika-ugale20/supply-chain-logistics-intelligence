# Data

The project uses the DataCo Smart Supply Chain dataset.

Raw dataset files are not included in this repository. The analytical outputs used for Power BI are available in the `dashboard/` folder.
